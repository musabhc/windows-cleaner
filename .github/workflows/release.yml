name: release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  signed-release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Validate signing secrets
        shell: pwsh
        env:
          CODESIGN_CERTIFICATE_BASE64: ${{ secrets.CODESIGN_CERTIFICATE_BASE64 }}
          CODESIGN_CERTIFICATE_PASSWORD: ${{ secrets.CODESIGN_CERTIFICATE_PASSWORD }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:CODESIGN_CERTIFICATE_BASE64) -or [string]::IsNullOrWhiteSpace($env:CODESIGN_CERTIFICATE_PASSWORD)) {
            throw "Missing signing secrets. This workflow is fail-closed and will not publish an unsigned public release."
          }

      - name: Compute version
        id: version
        shell: pwsh
        run: |
          "version=0.1.${{ github.run_number }}" >> $env:GITHUB_OUTPUT

      - name: Build, sign, package, and publish
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CODESIGN_CERTIFICATE_BASE64: ${{ secrets.CODESIGN_CERTIFICATE_BASE64 }}
          CODESIGN_CERTIFICATE_PASSWORD: ${{ secrets.CODESIGN_CERTIFICATE_PASSWORD }}
          CODESIGN_TIMESTAMP_URL: ${{ secrets.CODESIGN_TIMESTAMP_URL }}
        run: |
          ./build/Invoke-Release.ps1 `
            -Version "${{ steps.version.outputs.version }}" `
            -RepositoryUrl "https://github.com/${{ github.repository }}"
